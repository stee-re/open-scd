# OpenSCD XML

This package is published to npm as `@compas-oscd/xml` and provides the XML utilities used by the OpenSCD codebase.

It is maintained in the `com-pas/open-scd` repository and is consumed by `compas-open-scd` as an npm dependency rather than via a forked shared source tree.
